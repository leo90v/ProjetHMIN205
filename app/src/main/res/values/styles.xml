<resources xmlns:and="http://schemas.android.com/apk/res-auto" xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name ="android:fontFamily">@font/font_quicksand</item>
        <item name="materialButtonStyle">@style/button</item>
        <item name="android:windowBackground">@drawable/bgc</item>
        <item name="editTextBackground">@null</item>
        <item name="android:textViewStyle">@style/textviewStyle</item>
        <item name="actionBarPopupTheme">@style/Theme.MaterialComponents.Light</item>
    </style>

    <style name="button" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/button_border</item>
    </style>

    <style name="textviewStyle" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/font_quicksand</item>
    </style>

    <style name="noTitle" parent="AppTheme">
        <item name="windowNoTitle">true</item>
    </style>

</resources>
